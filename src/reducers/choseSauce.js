import { ADD_SAUCE } from '../actions/cart'


export const choseSauce = (state = [], action = {} ) => {
    switch (action.type) {
            case 'ADD_SAUCE' :
                    return action.payload.price
                }
                return state;
            }